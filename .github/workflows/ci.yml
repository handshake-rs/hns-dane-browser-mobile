name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  scope:
    name: Change scope and repository policy
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    outputs:
      rust: ${{ steps.targets.outputs.rust }}
      android: ${{ steps.targets.outputs.android }}
      ios: ${{ steps.targets.outputs.ios }}
    steps:
      - name: Check out repository and comparison history
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Select affected CI targets
        id: targets
        env:
          CI_EVENT_NAME: ${{ github.event_name }}
          CI_BEFORE_SHA: ${{ github.event.before }}
          CI_CURRENT_SHA: ${{ github.sha }}
          CI_PR_BASE_SHA: ${{ github.event.pull_request.base.sha }}
          CI_PR_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: ./scripts/ci-changed-targets.sh

      - name: Install pinned Rust toolchain for metadata policy
        run: rustup toolchain install 1.92.0 --profile minimal

      - name: Verify repository policy and release metadata
        run: |
          python3 -m unittest -v tests/test_ci_changed_targets.py
          python3 -m unittest -v tests/test_release_safety.py
          python3 ./scripts/verify_android_translations.py
          ./scripts/verify-supply-chain.sh
          python3 ./scripts/generate-third-party-notices.py --check
          ./scripts/check-version-consistency.sh

  rust:
    name: Rust and supply chain
    needs: scope
    if: ${{ needs.scope.outputs.rust == 'true' }}
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - name: Check out repository
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
        with:
          persist-credentials: false

      - name: Install pinned Rust toolchain
        run: |
          rustup toolchain install 1.92.0 --profile minimal --component rustfmt --component clippy
          rustup default 1.92.0

      - name: Install pinned cargo-deny
        run: cargo +1.92.0 install cargo-deny --version 0.19.9 --locked

      - name: Run Rust, fuzz, tool, and supply-chain checks
        run: ./scripts/check.sh

  android:
    name: Android build and unit tests
    needs: scope
    if: ${{ needs.scope.outputs.android == 'true' }}
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - name: Check out repository
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
        with:
          persist-credentials: false

      - name: Set up JDK 21
        uses: actions/setup-java@c1e323688fd81a25caa38c78aa6df2d33d3e20d9 # v4.8.0
        with:
          distribution: temurin
          java-version: "21"

      - name: Install pinned Rust Android tools
        run: |
          rustup toolchain install 1.92.0 --profile minimal
          rustup default 1.92.0
          rustup target add --toolchain 1.92.0 \
            armv7-linux-androideabi \
            aarch64-linux-android \
            x86_64-linux-android
          cargo +1.92.0 install cargo-ndk --version 4.1.2 --locked

      - name: Install pinned Android SDK components
        run: |
          "${ANDROID_SDK_ROOT}/cmdline-tools/latest/bin/sdkmanager" \
            "platforms;android-37.0" \
            "build-tools;36.0.0" \
            "ndk;28.2.13676358"

      - name: Verify pinned build inputs
        run: ./scripts/verify-supply-chain.sh

      - name: Verify shared runtime boundaries
        run: ./scripts/check-runtime-boundaries.sh

      - name: Build and test Android variants
        working-directory: android
        env:
          ANDROID_NDK_HOME: /usr/local/lib/android/sdk/ndk/28.2.13676358
          ANDROID_NDK_ROOT: /usr/local/lib/android/sdk/ndk/28.2.13676358
          HNS_ANDROID_NDK_VERSION: 28.2.13676358
        run: ./gradlew assembleDebug testDebugUnitTest lintDebug verifyReleaseBundleStructure --no-daemon --stacktrace

      - name: Upload instrumented debug APK
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: hns-dane-browser-debug-${{ github.sha }}
          path: android/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
          retention-days: 3

  android-runtime-instrumentation:
    name: Android native runtime instrumentation
    needs: scope
    if: ${{ needs.scope.outputs.android == 'true' }}
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - name: Check out repository
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
        with:
          persist-credentials: false

      - name: Set up JDK 21
        uses: actions/setup-java@c1e323688fd81a25caa38c78aa6df2d33d3e20d9 # v4.8.0
        with:
          distribution: temurin
          java-version: "21"

      - name: Install pinned Rust Android tools
        run: |
          rustup toolchain install 1.92.0 --profile minimal
          rustup default 1.92.0
          rustup target add --toolchain 1.92.0 \
            armv7-linux-androideabi \
            aarch64-linux-android \
            x86_64-linux-android
          cargo +1.92.0 install cargo-ndk --version 4.1.2 --locked

      - name: Install pinned Android SDK components
        run: |
          "${ANDROID_SDK_ROOT}/cmdline-tools/latest/bin/sdkmanager" \
            "cmdline-tools;22.0" \
            "platforms;android-37.0" \
            "build-tools;36.0.0" \
            "ndk;28.2.13676358"
          # The action prepends cmdline-tools/latest, so promote the pinned
          # version over the runner's v12 directory that miswrites API 37 AVDs.
          mv "${ANDROID_SDK_ROOT}/cmdline-tools/latest" \
            "${RUNNER_TEMP}/android-cmdline-tools-preinstalled"
          ln -s "${ANDROID_SDK_ROOT}/cmdline-tools/22.0" \
            "${ANDROID_SDK_ROOT}/cmdline-tools/latest"
          [[ "$("${ANDROID_SDK_ROOT}/cmdline-tools/latest/bin/sdkmanager" --version)" == "22.0" ]]

      - name: Enable KVM group permissions
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' \
            | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      - name: Run native runtime and proof-details instrumentation regressions
        uses: ReactiveCircus/android-emulator-runner@e89f39f1abbbd05b1113a29cf4db69e7540cae5a # v2.37.0
        env:
          ANDROID_NDK_HOME: /usr/local/lib/android/sdk/ndk/28.2.13676358
          ANDROID_NDK_ROOT: /usr/local/lib/android/sdk/ndk/28.2.13676358
          HNS_ANDROID_NDK_VERSION: 28.2.13676358
        with:
          api-level: "37.0"
          target: google_apis
          arch: x86_64
          channel: stable
          pre-emulator-launch-script: adb start-server
          working-directory: android
          script: >-
            ./gradlew :app:connectedDebugAndroidTest
            --no-daemon
            --stacktrace
            "-Pandroid.testInstrumentationRunnerArguments.class=com.denuoweb.hnsdane.net.SharedBrowserNamespacePolicyInstrumentationTest#freshAndroidStorageOpensTheNativeRuntime,com.denuoweb.hnsdane.ui.HnsProofDetailsActivityInstrumentationTest"

  ios:
    name: Apple ABI, XCFramework, and iOS shell
    needs: scope
    if: ${{ needs.scope.outputs.ios == 'true' }}
    runs-on: macos-26
    # Three release-mode Rust slices plus simulator XCTest and a device-slice
    # Release link routinely exceed the old one-hour budget on the standard
    # 3-core hosted runner even when every command is making progress.
    timeout-minutes: 90
    steps:
      - name: Check out repository
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
        with:
          persist-credentials: false

      - name: Run the complete iOS gate
        id: ios_gate
        run: ./scripts/run-ios-gate.sh

      - name: Upload failed iOS gate diagnostics
        if: failure() && steps.ios_gate.outcome == 'failure'
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: ios-gate-diagnostics-${{ github.sha }}-attempt-${{ github.run_attempt }}
          path: build/ios-gate-diagnostics
          if-no-files-found: warn
          retention-days: 7

  required:
    name: Required CI
    needs: [scope, rust, android, android-runtime-instrumentation, ios]
    if: ${{ !cancelled() }}
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Verify every selected target passed
        env:
          SCOPE_RESULT: ${{ needs.scope.result }}
          RUST_REQUIRED: ${{ needs.scope.outputs.rust }}
          RUST_RESULT: ${{ needs.rust.result }}
          ANDROID_REQUIRED: ${{ needs.scope.outputs.android }}
          ANDROID_RESULT: ${{ needs.android.result }}
          ANDROID_RUNTIME_INSTRUMENTATION_RESULT: ${{ needs.android-runtime-instrumentation.result }}
          IOS_REQUIRED: ${{ needs.scope.outputs.ios }}
          IOS_RESULT: ${{ needs.ios.result }}
        run: |
          set -euo pipefail
          failed=0

          if [[ "$SCOPE_RESULT" != "success" ]]; then
            echo "::error::Change scope and repository policy finished with $SCOPE_RESULT."
            failed=1
          fi

          check_target() {
            local name="$1"
            local required="$2"
            local result="$3"
            if [[ "$result" == "failure" || "$result" == "cancelled" ]]; then
              echo "::error::$name finished with $result."
              failed=1
            elif [[ "$required" == "true" && "$result" != "success" ]]; then
              echo "::error::$name was required but finished with $result."
              failed=1
            fi
          }

          check_target "Rust" "$RUST_REQUIRED" "$RUST_RESULT"
          check_target "Android" "$ANDROID_REQUIRED" "$ANDROID_RESULT"
          check_target \
            "Android native runtime instrumentation" \
            "$ANDROID_REQUIRED" \
            "$ANDROID_RUNTIME_INSTRUMENTATION_RESULT"
          check_target "iOS" "$IOS_REQUIRED" "$IOS_RESULT"
          exit "$failed"
