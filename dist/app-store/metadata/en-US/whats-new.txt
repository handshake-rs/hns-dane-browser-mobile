Fixes secure-browser startup on fresh iOS installs when the default protection policy is already active. Safe page loads now recover from brief connection losses during validated Handshake header maintenance. This update also keeps browsing responsive during sync, publishes header and peer state atomically, and adds hardened recovery for confirmed DNS interception.
