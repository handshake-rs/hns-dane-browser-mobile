Fixes secure-browser startup on fresh iOS installs when the default protection policy is already active. Also keeps browsing responsive during validated Handshake header sync, publishes header and peer state atomically, and adds hardened recovery for confirmed DNS interception.
